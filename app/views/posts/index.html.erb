<p id="notice"><%= notice %></p>

<h1>Posts</h1>

 
    <% @posts.each do |post| %>
      <ul>
        <li> <%= user_check(post, @user) %></li>
        <li><%= post.title %></li>
        <li><%= post.body %></li>
      </ul>
    <% end %>

<br>

<%= new_post_btn%>
